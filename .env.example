# Fill after running the deploy script in contracts/ — until then the site runs in demo mode.
# Testnet: forge script script/DeployTestnet.s.sol --rpc-url https://rpc.testnet.chain.robinhood.com --broadcast
VITE_PACK_SALE_ADDRESS=
VITE_VAULT_ADDRESS=
# Defaults to real USDG on Robinhood Chain mainnet; override with the mock address on testnet.
VITE_USDG_ADDRESS=
